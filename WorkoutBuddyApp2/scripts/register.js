var app = app || {};
var el = el || {};

app.Register = (function () {
    'use strict'

    var registerModel = (function () {
        
        
        var init = function () {
            
        };
        
        var show = function () {
        };

        return {
            init: init,
            show: show
        };
        
    }());
    
    return registerModel;
    
}());